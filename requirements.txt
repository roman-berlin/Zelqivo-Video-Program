# Core dependencies (CPU-only operation)
# Install with: pip install -e .
numpy
PyQt6
pyqt6-sip
opencv-python
ffmpeg-python
moviepy
tqdm

# AI extras (optional - enables real diarization and advanced audio sync)
# Install with: pip install -e ".[ai]"
# Uncomment below or use: pip install torch librosa soundfile pydub pyannote.audio speechbrain
# torch
# librosa
# soundfile
# pydub
# pyannote.audio
# speechbrain

# Development dependencies
# Install with: pip install -e ".[dev]"
pytest
pytest-cov
black
ruff
mypy
pyinstaller
