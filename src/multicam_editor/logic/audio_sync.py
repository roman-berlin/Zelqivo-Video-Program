"""External audio synchronisation module."""

from typing import List, Optional


def sync_external_audio(external_audio: str, video_audio_paths: List[str]) -> Optional[str]:
    """
    Synchronise an external audio track with video audio (stub).
    Returns None for now.
    """
    return None
